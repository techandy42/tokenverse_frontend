import React from 'react'

const CreateMany: React.FC<any> = ({
  collection,
  setCollection,
  forms,
  setForms,
  fileUrls,
  setFileUrls,
}) => {
  return (
    <div>
      <h1>Create Many</h1>
    </div>
  )
}

export default CreateMany
