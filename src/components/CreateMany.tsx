import React from 'react'

const CreateMany: React.FC<any> = ({
  isCreateMultiple,
  tokenType,
  forms,
  setForms,
  clearCounter,
}) => {
  return (
    <div>
      <h1>Create Many</h1>
    </div>
  )
}

export default CreateMany
